package tz.go.tirdo.teltp.billing.entity;

/** Payment channels per Module 5. Each is realized by a PaymentMethod seam implementation. */
public enum PaymentChannel { GEPG, MOBILE_MONEY, BANK_TRANSFER }
