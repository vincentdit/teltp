package tz.go.tirdo.teltp.auth.entity;

/** Top-level personas per the client's Module 1 role list. */
public enum RoleName {
    ADMIN,
    INSTRUCTOR,
    STUDENT,
    CORPORATE_CLIENT,
    FINANCE_OFFICER
}
