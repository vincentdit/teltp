package tz.go.tirdo.teltp.notification.entity;

public enum NotificationType {
    ENROLLMENT_CONFIRMED, PAYMENT_CONFIRMED, ASSESSMENT_GRADED,
    CERTIFICATE_READY, EVENT_REMINDER, DEADLINE_REMINDER, GENERAL
}
